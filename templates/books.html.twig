{% extends 'base.html.twig' %}
    {% block content %}
        <div class=" articles">
        {% for book in books %}
            <div class = "article">
                <h1>{{ book.title|upper }}</h1>
                <p>Auteur : {{ book.author|upper }}</p>
                <p>Resumé : {{ book.resume }}</p>
                <p>{{ book.nbPages }}</p>
                <a href="{{ path ('book', {'id': book.id }) }}">Details</a>
                <br/>
                <a href="{{ path ('book_delete', {'id': book.id }) }}">effacer livre</a>
            </div>
        {% endfor %}
        </div>
    {% endblock %}


