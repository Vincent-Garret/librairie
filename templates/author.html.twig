{% extends 'base.html.twig' %}
{% block content %}
    <div class = "article">
        <h1>{{ authors.name|upper}}</h1>
        <p>{{ authors.fistName|upper }}</p>
        <p>{{ authors.birthDate }}</p>
        <p>{{ authors.deathDate }}</p>
        <p>{{ authors.biography }}</p>
    </div>
{% endblock %}

